# Copy to .env in the project root. Used by the FastAPI backend.
OPENAI_API_KEY=
# Optional; only if you use a proxy or non-default API host (OpenAI SDK base_url).
# OPENAI_BASE_URL=
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_URL_ENDPOINTS=